# 这是AI服务配置的模板文件。
# 请将此文件复制为 .env 并填入您的真实配置信息。
# .env 文件不应被提交到版本控制中。

# 必需：您的 API 密钥
OPENAI_API_KEY="your_openai_api_key_here"

# 可选：自定义 API 的 URL (例如，本地模型或其他云服务)
# 如果留空，将默认使用 https://api.openai.com/v1
OPENAI_API_BASE="https://api.openai.com/v1"

# 可选：自定义模型名称
# 如果留空，将默认使用 gpt-3.5-turbo
OPENAI_MODEL_NAME="gpt-3.5-turbo"